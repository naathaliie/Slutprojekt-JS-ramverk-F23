export type oneBook = {};

export type oneAuthor = {
  
};

export type fetchedData = {
  docs: [];
  numFound: number;
  numFoundExact: boolean;
  num_found: number;
  offset: any;
  q: string;
  start: number;
};
